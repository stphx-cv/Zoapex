using System.Windows;

namespace Zoapex.Presentation;

// Ventana generada por la plantilla — no usada (ver Views/MainWindow.xaml)
public partial class MainWindow : Window
{
    public MainWindow() => InitializeComponent();
}
