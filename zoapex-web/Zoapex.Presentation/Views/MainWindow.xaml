<Window x:Class="Zoapex.Presentation.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Zoapex — Pet Store"
        Height="700" Width="1150"
        MinHeight="600" MinWidth="950"
        WindowStartupLocation="CenterScreen">
    <DockPanel>

        <!-- Barra de navegación superior -->
        <Border DockPanel.Dock="Top" Background="#2C3E50" Padding="20,10">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center">
                    <TextBlock Text="&#x1F43E;" FontSize="22" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="Zoapex" Foreground="White" FontSize="20" FontWeight="Bold"
                               VerticalAlignment="Center"/>
                    <TextBlock Text="Pet Store" Foreground="#95A5A6" FontSize="13"
                               VerticalAlignment="Center" Margin="8,0,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="btnCatalog" Content="Catalog"
                            Style="{StaticResource NavButton}" Background="#3498DB"
                            Click="BtnCatalog_Click"/>
                    <Button x:Name="btnCart" Content="Cart"
                            Style="{StaticResource NavButton}" Background="#27AE60"
                            Click="BtnCart_Click"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Frame principal de navegación entre vistas -->
        <Frame x:Name="mainFrame" NavigationUIVisibility="Hidden"/>
    </DockPanel>
</Window>
